{% extends 'base.html.twig' %}

{% block title %}Panel{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
    <h1>Panel</h1>
    <table class="table">
        <thead>
            <th>Gestion des utilisateurs</th>
            <th>Gestion des Produits</th>
            <th>Gestion des commandes</th>
            <th>Gestion des Stocks</th>
        </thead>
        <tbody>
            <tr>
                <td><a class="btn btn-success" href="{{ path('app_utilisateurs')}}">Utilisateurs</a></td>
                <td><a class="btn btn-success" href="{{ path('app_utilisateurs')}}">Produits</a></td>
                <td><a class="btn btn-success" href="{{ path('app_commandes')}}">Commandes</td>
                <td><a class="btn btn-success" href="{{ path('app_stock') }}">Stocks</td>
            </tr>
        <tbody>
    </table>
    </div>
    <a href="{{ path ('app_produits') }}" class="btn btn-primary">Retour au magasin</a>
</div>
{% endblock %}